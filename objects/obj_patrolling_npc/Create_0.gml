

// Скорость перемещения NPC
speed = 2;

// Направление движения (начальное)
direction = 0; // 0 градусов - вправо

// Расстояние до поворота (случайное)
path_distance = irandom_range(500, 1050);

// Таймер для изменения направления (опционально, для более реалистичного движения)
timer = 0;
timer_max = irandom_range(10, 30); // Интервал в шагах